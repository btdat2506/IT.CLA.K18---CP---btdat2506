#include <bits/stdc++.h>

using namespace std;

#define ll int64_t
#define For(i, a, b) for(ll i = a; i <= b; i++)

ll n, db[300][300], xuli[300][300];

void process()
{
	For(i, 1, n)
	F
}

void reset()
{
	memset(db, 0, sizeof(db));
	memset(xuli, 0, sizeof(xuli));
	n = 0;
}

void input()
{
	cin >> n;
	For(i, 1, n)
	For(j, 1, n)
	cin >> db[i][j];
}

int main()
{
	freopen("MATRIX.INP", "r", stdin);
	freopen("MATRIX.OUT", "w", stdout);
	ios::sync_with_stdio(0); cin.tie(0); cout.tie(0);
	input();
	process();
}