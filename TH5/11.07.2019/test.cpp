#include <bits/stdc++.h>

using namespace std;

void process()
{
	long long a[100010];
	memset(a, 0, sizeof(a));
	for(long long i = 0; i < 100010; i++)
	if (i > 100000) cout << i << endl;
}

int main()
{
	freopen("output.txt", "w", stdout);
	process();
}