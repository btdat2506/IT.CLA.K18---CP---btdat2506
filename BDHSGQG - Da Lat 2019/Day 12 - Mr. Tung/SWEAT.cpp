#include <bits/stdc++.h>

using namespace std;

typedef int64_t ll;
#define For(i, a, b) for(ll i = a; i <= b; i++)

void process()
{

}

int main()
{
    freopen("SWEAT.INP", "r", stdin);
    freopen("SWEAT.OUT", "w", stdout);
    cin >> n >> lb >> re >> k;
    if (k == n)
    
}