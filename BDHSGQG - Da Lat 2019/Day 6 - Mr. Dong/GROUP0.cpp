#include <bits/stdc++.h>

using namespace std;

#define ll int64_t
#define For(i, a, b) for(ll i = a; i <= b; i++)

void process()
{
	
}

void input()
{
	cin >> n >> k;
	For(i, 1, n)
	cin >> a[i];
}

int main()
{
	freopen("GROUP0.INP", "r", stdin);
	freopen("GROUP0.OUT", "w", stdout);
	input();
	process();
}