#include <bits/stdc++.h>

using namespace std;

#define ll int64_t
#define For(i, a, b) for(ll i = a; i <= b; i++)
#define Fod(i, a, b) for(ll i = a; i >= b; i--)

ll n, k[20];

void process()
{
	ll lengst = st;
	st = ' ' + st;
	For(len, 1, lengst/2)
	For(i, 2, st.length()-1 - len*2)
	{
		
	}
}

void input()
{
	cin >> n;
	cin >> st;
	For(i, 1, n) cin >> k[i];
}

int main()
{
	freopen("input.txt", "r", stdin);
	freopen("output.txt", "w", stdout);
	input();
	process();	
}