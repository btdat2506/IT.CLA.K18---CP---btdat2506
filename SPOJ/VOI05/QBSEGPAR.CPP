#include <bits/stdc++.h>

using namespace std;

typedef int64_t ll;
#define For(i, a, b) for(ll i = a; i <= b; i++)

//9:38 am, Oct 05 2020

ll n, k, arr[15010], f[15010][15010];

void process()
{
    
}

void input()
{
    cin >> n >> k;
    For(i, 1, n)
    cin >> arr[i];
}

int main()
{
    freopen("test.in", "r", stdin);
    freopen("test.ok", "w", stdout);
    input();
    process();
}